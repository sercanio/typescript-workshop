{
  "scripts": {
    "compiler": "nodemon --watch main.ts --exec cross-env TS_NODE_PROJECT='./tsconfig.json' tsc --noEmitOnError",
    "watcher": "nodemon ./dist/main.js",
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --poll src/main.ts"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.2"
  }
}
